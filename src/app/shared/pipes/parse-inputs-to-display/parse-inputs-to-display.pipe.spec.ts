import { ParseInputsToDisplayPipe } from './parse-inputs-to-display.pipe';

describe('ParseInputsToDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseInputsToDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
